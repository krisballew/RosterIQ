# Hoist all @supabase/* to top-level node_modules so TypeScript can resolve
# @supabase/auth-js (and other sub-packages) from @supabase/supabase-js type
# declarations. Without this, Vercel's pnpm install leaves auth-js only in the
# .pnpm virtual store, causing "Property getUser does not exist on SupabaseAuthClient".
public-hoist-pattern[]=@supabase/*
